{
  "name": "stripe-shopify-webhook",
  "version": "1.0.0",
  "description": "Segna automaticamente gli ordini Shopify come pagati quando Stripe conferma il pagamento.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "stripe": "^16.2.0",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
